import { describe, it, expect, jest } from "@jest/globals";
import { processEvents } from "../../src/pro-add01/08-processEvents";

describe("processEvents", () => {
  it.todo("ケース1を書く");
  it.todo("ケース2を書く");
  it.todo("ケース3を書く");
});

// ヒント:
// 重複なし→processedカウント、重複あり→skippedカウント・appendしない、混在ケース
