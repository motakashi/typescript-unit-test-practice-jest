{
  "name": "typescript-unit-test-practice-jest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest template --runInBand",
    "test:watch": "jest template --watch",
    "test:answers": "jest answers --runInBand",
    "coverage": "jest template --coverage --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.8.0"
  }
}
